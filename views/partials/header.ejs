<header>Header</header>
<nav>
    <ul>
        <% transMenu.forEach((item) => { %>
            <% if (item.link == '/') { %>
            <li>
                <a href="<%= item.link %>"><%= item.label %></a>
            </li>
            <% } %>
        <% }) %>
    </ul>
</nav>
